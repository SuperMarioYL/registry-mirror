name: Mirror Docker Images / 镜像同步请求
description: Submit a list of Docker images to mirror / 提交需要同步的镜像列表
title: "[mirror]"
labels: ["mirror-request"]
body:
  - type: markdown
    attributes:
      value: |
        ## Image Mirror Request / 镜像同步请求

        Enter the Docker images you want to mirror, one per line. Each image **must** include a tag.
        请输入需要同步的镜像，每行一个，每个镜像**必须包含 tag**（如 `nginx:latest`）。
  - type: textarea
    id: images
    attributes:
      label: Images / 镜像列表
      description: |
        One image per line. Format: [registry/][namespace/]image:tag
        每行一个镜像，格式：[仓库地址/][命名空间/]镜像名:tag
      placeholder: |
        nginx:latest
        gcr.io/google-containers/pause:3.9
        registry.k8s.io/kube-apiserver:v1.28.0
        quay.io/prometheus/node-exporter:v1.7.0
    validations:
      required: true
  - type: textarea
    id: platforms
    attributes:
      label: Platforms / 平台列表（可选）
      description: |
        Optional. Provide target platforms separated by comma or new line.
        可选。目标平台支持逗号或换行分隔。
        Leave empty to use default `linux/amd64`.
        留空则默认使用 `linux/amd64`。
      placeholder: |
        linux/amd64
        linux/arm64
        linux/arm/v7
    validations:
      required: false
